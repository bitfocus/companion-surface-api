{
    "name": "@companion-surface/api",
    "license": "MIT",
    "private": true,
    "workspaces": [
        "packages/*"
    ],
    "scripts": {
        "build": "run build:schema && run build:root",
        "build:clean": "(rm -rf ./packages/*/dist ./packages/*/*.tsbuildinfo || true) && run build",
        "build:root": "tsc --build tsconfig.build.json",
        "build:schema": "cd packages/base && node ./tools/generate-schemas.mjs",
        "dev": "run build:root --watch",
        "lint": "eslint .",
        "unit": "vitest run",
        "unit:watch": "vitest"
    },
    "engines": {
        "node": "^22.21 || ^26.5"
    },
    "devDependencies": {
        "@companion-module/tools": "^3.0.2",
        "@tsconfig/node22": "^22.0.5",
        "@types/node": "^22.20.1",
        "eslint": "^10.8.0",
        "prettier": "^3.9.6",
        "typescript": "~6.0.3",
        "typescript-eslint": "^8.65.0",
        "vitest": "^4.1.10",
        "vitest-mock-extended": "^4.0.0"
    },
    "packageManager": "yarn@4.18.0"
}
